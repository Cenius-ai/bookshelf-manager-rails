<%= turbo_stream.replace "loan_status" do %>
  <%= render "books/loan_status", book: @book, user_loan: @loan, error: nil %>
<% end %>

<%= turbo_stream.replace "flash" do %>
  <div class="flash-notice">Book borrowed successfully. Due <%= @loan.due_date.strftime("%b %d, %Y") %>.</div>
<% end %>
